bool searchInARotatedSortedArrayII(vector<int>&A, int key) {
    // Write your code here.
    int size=A.size();

    for(int i=0;i<size;i++){
        if(A[i]==key)
           return true;
    }
    return false;
}
